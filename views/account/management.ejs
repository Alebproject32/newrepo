<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body>
    <main>
      <div id="wrapper">
        <h1 class="account-management-h1"><%= title %></h1>

        <!-- Flash Messages -->
        <%- messages() %> <% if (errors) { %>
        <ul class="notice">
          <% errors.array().forEach(error => { %>
          <li><%= error.msg %></li>
          <% }) %>
        </ul>
        <% } %>
        <div class="account-management-container">
          <div class="welcome-message">
            <h2>Welcome, <%= accountData.account_firstname %>!</h2>
            <p>You're logged in.</p>
            <ul>
              <li>
                Name: <%= accountData.account_firstname %> <%=
                accountData.account_lastname %>
              </li>
              <li>Email: <%= accountData.account_email %></li>
              <li>Account Type: <%= accountData.account_type %></li>
            </ul>
          </div>
          <div class="account-actions">
            <h3>Account Management</h3>
            <p>Use the link below to update your account information.</p>
            <!-- NEW: Link to the account update view -->

            <a
              href="/account/update/<%= accountData.account_id %>"
              title="Click to update account information"
              >Update Account Information</a
            >
          </div>
          <!-- NEW: Conditional Inventory Management Link (Task 3 Requirement) -->
          <% if (accountData.account_type === 'Admin' ||
          accountData.account_type === 'Employee') { %>
          <div class="inventory-management">
            <h2>Inventory Management</h2>
            <p>You have access to manage inventory.</p>
            <a href="/inv/" title="Click to manage inventory"
              >Inventory Management</a
            >
          </div>
          <% } %>
        </div>
      </div>
    </main>
  </body>
</html>
